# Chat model — any OpenAI-compatible endpoint. Leave API_BASE empty for OpenAI.
API_KEY=sk-your-key-here
API_BASE=
MODEL=gpt-4o-mini

# Embeddings run locally by default (free, no key). Set EMBED_API=1 to use the API.
# EMBED_API=1
# EMBED_MODEL=text-embedding-3-small
